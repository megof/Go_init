{{ define "lgn.tm" }}
{{ template "hea.tm" }}
<div class="container"> 
    <form method="POST" action="/login">
        <div class="form-group">
            <label for="">Nombre</label>
            <input type="text" class="form-control" name="Nameus" id="Nameus" aria-describedby="helpId" placeholder="">
            <small id="helpId" class="form-text text-muted">Inserta el nombre de usuario</small>
        </div>
        <div class="form-group">
            <label for="">Inserta la contraseña de usuario</label>
            <input type="password" class="form-control" name="Passus" id="Passus" placeholder="">
        </div>
        <div class="form-group row">
            <div class="offset-sm-2 col-sm-10">
                <button type="submit" class="btn btn-primary">Login</button>
            </div>
        </div>
    </form>
</div>
{{ template "foo.tm" }}
{{ end }}